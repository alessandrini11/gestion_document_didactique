{% extends 'base.html.twig' %}

{% block title %}Edit Personne{% endblock %}

{% block body %}
    <div class="w-10/12 md:1/2 mx-auto">
        <h1 class="text-2xl">Modifier une Personne</h1>

        {{ include('personne/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
    <a href="{{ path('personne_index') }}">back to list</a>

    {{ include('personne/_delete_form.html.twig') }}
{% endblock %}
